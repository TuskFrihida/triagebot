# credentials/

Put the Google service-account JSON key here, e.g. `credentials/google-service-account.json`,
then point `GOOGLE_SERVICE_ACCOUNT_FILE` at it in your `.env`.

This entire directory is listed in `.gitignore`. Nothing inside it is ever committed
except this README.
